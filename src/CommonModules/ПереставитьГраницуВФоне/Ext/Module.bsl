
Процедура ПереставитьГраницуВФоне(ДатаИтогов) Экспорт  
	ТекДата = ТекущаяДата();
	Пока ТекДата + 5 < ТекущаяДата() Цикл
	КонецЦикла;
	РегистрыБухгалтерии.РегистрБухгалтерии1.УстановитьМаксимальныйПериодРассчитанныхИтогов(ДатаИтогов);
КонецПроцедуры

Процедура ПереставитьГраницуВФонеСБлокировкой(ДатаИтогов) Экспорт  
	ТекДата = ТекущаяДата();
	Пока ТекДата + 5 < ТекущаяДата() Цикл
	КонецЦикла;
	НачатьТранзакцию();
	
		Блокировка = Новый БлокировкаДанных;
		ЭлементБлокировки = Блокировка.Добавить("РегистрБухгалтерии.РегистрБухгалтерии1.НаборЗаписей");
		ЭлементБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
		Блокировка.Заблокировать();        
	
		РегистрыБухгалтерии.РегистрБухгалтерии1.УстановитьМаксимальныйПериодРассчитанныхИтогов(ДатаИтогов);
		
	ЗафиксироватьТранзакцию();
КонецПроцедуры